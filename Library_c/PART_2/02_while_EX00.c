#include<stdio.h>

int main(){
    
    int num = 0;
    while(num <=120){
        // O valor 3 antes do símbolo do tipo de dado esperado que será
        // mostrado no console, reserva espaços no console e alinha a
        // direita os valroes imprimidos.
        printf("%3d\n", num);
        num++;
    }
}